#include <stdio.h>
#include <stdlib.h>

//convert characters to integrators
void char_to_int(void)
{
   num_word = atoi((num + n)->words);
   lev = atoi((num + n)->level);
   score_ = atoi((num + n)->score);
}

//convert integrators to characters
void int_to_char(void)
{
    itoa(num_word,(num + n)->words,10);
    itoa(lev,(num + n)->level,10);
    itoa(score_,(num + n)->score,10);
}
